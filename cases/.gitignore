# 조사서 원본은 git 에 넣지 않는다. 사건 정보·개인정보가 들어 있을 수 있다.
raw/*
!raw/.gitkeep

# 변환 결과도 재생성물이므로 넣지 않는다. 원본에서 다시 만들 수 있다.
abox/*
!abox/.gitkeep

# 관찰만 떼어낸 텍스트도 원본에서 재생성된다. 사건 정보가 남아 있으므로 제외.
observed/*
manifest.json
sessions.json
sessions/
