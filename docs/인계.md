# 인계 — 다음 세션이 읽을 것

작성 2026-09-02 · 기준 커밋 `7916c0c`

`CLAUDE.md` 를 먼저 읽고 이 문서를 읽는다. `CLAUDE.md` 는 **규칙**이고 이 문서는
**지금 어디까지 왔고 왜 그렇게 됐는가**다. 숫자는 `make status` 가 만든다 —
여기 적은 값은 기준일 스냅숏이므로 다르면 `make status` 를 믿는다.

---

## 1. 이 프로젝트가 무엇을 하는가

전기화재 발화 메커니즘 판정 온톨로지. 목표는 **NFPA 921 과 국내 실무의 화재조사
이론을 오류 없이 지식 그래프로 옮기는 것**이다. 정확도는 목표가 아니라 **이식이
제대로 됐는지 보는 계기**다. 이번 세션에도 원문대로 고쳐서 정확도가 내려간 일이
있었고 그대로 두었다(4.2).

다섯 요인을 가른다 — 접촉불량 · 기계적 손상(압착) · 반단선 · 절연열화 · 트래킹.
여섯 번째 가설로 외부화염이 있으나 사례가 0건이다.

**NFPA 921 은 판정 숫자에 거의 영향을 주지 않는다.** 점수를 만드는 지표 규칙은 논문
Table 2 와 국내 실무(실무Ⅳ)에서 오고, NFPA 는 절차 층(반증 우선, 동점이면 원인미상,
소거법 금지 등 C-·D- 도형)이다. 사례 평가에서 NFPA 가 보이는 자리는 "둘 이상 남으면
원인미상"(§19.6.5.1) 하나다. NFPA 이식률과 정확도는 서로 다른 것을 잰다.

---

## 2. 지금 상태 (기준일 스냅숏)

```
정확도        82.7%   오판 4.0%   답한 것 중 95.4%   상한 90.0%
  압착손상 96.7 / 상한 100.0      트래킹 90.0 / 96.7
  접촉불량 80.0 / 83.3            절연열화 80.0 / 90.0
  반단선  66.7 / 80.0

NFPA 921 이식률    절 16/25 = 64%,  하위 절 61/80 = 76%
국내 표 2-1 이식률  갈래 8/16 = 50%  (없음 0 — 셋을 원문 확인 후 범위 밖으로)
실무Ⅳ 제2편 절 대조  절 17/29 = 59%,  하위 절 60/92 = 65%,  없음 8 → 후보 7  (make silmu)
Babrauskas 전기 절 대조  절 10/18 = 56%,  하위 절 60/69 = 87%,  실무Ⅳ와 일치 60 · 상충 12 · 보강 14 → 후보 7  (make babrauskas)
TBox              트리플 3959 · 클래스 241 · 지표 규칙 71 · SHACL 도형 80
검사              62개 통과 · DL 일관성 통과 · lint 0 · 두 축 편향 미검토 0
완성도 래칫        8개 전부 닫힘. BASELINE 이 비었다
판독 차이         사전 vs LLM +0.7%p — 판독은 병목이 아니다
반증              규칙 19개 중 사례에서 발동하는 것 0개 (외부화염 것 하나뿐, 사례 0건)
```

세션 시작 시점은 82.0% 였다. 82.0 → 82.7(판독) → 83.3(판독) → 79.3(원문대로 고침)
→ 82.7(대상 슬롯). 오르내림의 이유는 4장에 있다.

---

## 3. 명령

```bash
make test        # 정합성 (62개 불변식)
make status      # 위 숫자 전부
make reason      # DL 일관성 (HermiT, Java 필요)
make lint        # 사슬에 안 붙은 어휘
make calibrate   # 유도 가감점 vs 실측 변별력
make readers     # 사전 판독 vs LLM 판독 — 판독의 한계를 온톨로지의 한계와 가른다
make refute      # 반증 규칙·후보를 사례로 검증 — 정답 라벨에서 발동하면 반례다
make domestic    # 국내 공식 분류(표 2-1) 이식률
make build       # 시각화·검토표
python scripts/eval.py        # 정확도 · 상한 · 남은거리 · 핵심 단서 교차표
python scripts/cause_audit.py # 원인 축 편향 점검
python scripts/audit.py       # 형태 축 편향 점검
```

TTL 을 고치면 반드시 `python scripts/score.py --write` → `make test`.
`slots.py` 를 고치면 `python scripts/slots.py && python scripts/enrich.py`.

---

## 4. 이번 세션에서 결정한 것 — 왜 그렇게 됐는가

### 4.1 반증은 감점이고, 기각은 결정적 반증만 한다

SHACL F-1 은 반증 단서 하나로 곧바로 기각했고 Python 은 감점 후 40 미만이면 약화였다.
논문 3.2 는 "물리적으로 결정적인 사실만 기각 수준"이고 나머지는 가감점 누적이라
Python 쪽에 맞췄다. `test_verdicts_match_in_both_engines` 가 두 엔진의 판정을 견준다.

고치다가 더 큰 것이 나왔다. **P3 "반증이 점수보다 먼저"는 도형 간에 보장된 적이
없었다.** pySHACL 은 규칙의 `sh:order` 를 도형 안에서만 지키고 도형끼리는 도형의
`sh:order` 로 정렬한다. 도형에 값이 없어 F-4(점수)가 F-1(반증)보다 먼저 돌았다.
규칙 도형 25개 전부에 도형 수준 `sh:order` 를 넣었고
`test_rule_shapes_declare_execution_order` 가 지킨다.

### 4.2 습기는 트래킹과 절연열화 양쪽의 표지다 — 원문대로 고쳐 정확도가 내려갔다

실무Ⅳ 전체를 검색하니 습기·먼지는 절연열화의 원인 목록 ①이다(p.196 배선용차단기,
p.201 누전차단기, p.289 콘덴서). 이어서 "절연열화로 인한 발화형태는 트래킹과
흑연화"(p.196)라 한다. 두 요인의 경계는 p.149 의 **관례** — "전기기계·기구에 나타나는
경우를 트래킹" — 이고, 절연열화 단락은 "전선 피복이 오래되어 심선이 접촉하여
단락"(p.305)이다. 그래서 습기·분진 규칙을 절연열화에도 두었다(R_ID_sup7·8, k=2).
습기만 적힌 조사서에서는 두 가설이 동점이 되는 것이 맞다. 83.3 → 79.3.

이 결론에 이르기까지 두 번 뒤집었다(7장 ⑪). 처음엔 사슬 주석만 보고 붙였다가
트래킹이 10% 로 무너져 되돌리며 "p.245 는 모터 한정"이라 적었고, 그다음 PDF 전체를
검색해서 p.196·201 을 찾았다. **원칙("점수는 원문이 그 요인의 표지로 열거한 가설만
받고, 사슬의 양립 수 k 는 크기만 감쇠한다")은 맞았고 '열거됐는가'의 답이 틀렸다.**
`scripts/score.py` 머리말에 적었다.

### 4.3 트래킹의 자리와 대상 슬롯 — 79.3 → 82.7

p.147 은 트래킹을 "전압이 인가된 이극 도체 사이 고체절연물 표면"의 현상으로 정의하고,
표 2-1(p.127)은 자리를 "각종 스위치류 양극간"으로, p.201 은 "접속단자나 몰드케이스의
절연체에 먼지 또는 습기"로 적는다. 조사서도 같다 — 트래킹 29건 중 18건이 습기를
기기(분전반·차단기·기판·단자)와 함께 적고 절연열화는 전부 전선·코드와 적는다.

- **`InterPoleInsulatingSurface`**(이극 도체 간 절연물 표면). 반단선의 응력 집중 개소처럼
  '원인이 아니라 위치'인 선행 조건이며 트래킹 전용(k=1, +15)이다.
- **대상 슬롯 `origin_object`.** dataset.xlsx 에는 없다. 관찰 전문(`cases/observed`)에서
  '전기적 특이점이 식별된 문장'의 대상을 사전으로 읽는다(`slots.py origin_object`).
  특이점 낱말 바로 앞의 명사를 잡되 기기·단자가 문장에 있으면 그것을 우선한다 —
  트래킹은 기기 절연 표면에서 일어나고 녹는 것은 그 안의 전선이다. 특이점 문장이
  없으면 발화지점 문장으로 잡는다. 150건 중 121건에서 읽혔고 라벨은 보지 않는다.
- **TBox 의 `OriginSite`** 아래 일곱 클래스(기기 내부 절연부·접속단자·접속부·배선기구·
  권선·콘덴서·전선 중간부). 지표 규칙이 없어 단독으로는 점수를 만들지 않는다 —
  대상 어휘는 다섯 요인에 고루 나온다. cause_audit 에 일곱 개 판정을 적었다.
- **D-13 `TrackingSiteDerivationRuleShape`.** 기기 내부·단자 사실 + 오염 환경 확인이면
  그 대상 사실에 자리 유형을 덧붙인다. **새 사실을 만들지 않는다(C-4)** — F-2 가
  아크흔에 1차·2차를 덧붙이는 방식이다. Python 은 `Session.derive()` 가 같은 일을 하고
  (`Fact.derived_types`, 직렬화 안 함) `test_tracking_site_is_derived_in_both_engines`
  가 두 엔진을 견준다. 꼬임·결선(한 극의 이음)과 배선기구는 도출에 쓰지 않는다.

**붙이지 않은 것.** 반단선의 자리(플러그 접속부 부근, p.149)는 p.253 이 접촉불량의
자리로도 들어 대칭이 맞고, 대칭이면 정확도가 그대로라 붙이지 않았다. 반단선에만
주면 오르지만 그것은 근거 없는 비대칭이다.

### 4.4 판독기 수정 셋 (온톨로지가 아니라 판독)

- "늘어남과 줄어듦이 반복" → 인장. 반단선 1건. (`늘어[남나]`)
- 부재 확인이 5건뿐인 이유는 조사서가 부재를 거의 안 적기 때문이다(부재 어휘가 든
  슬롯이 사실 639개 중 8개). 그중 3건은 오독이었다 — "노후 선로로 교체 이력 없음"을
  노후 부재로, "용융흔 식별됨, 내부 부품은 특이점 없음"을 용융흔 부재로. 확인 상태를
  슬롯 전체가 아니라 그 표현이 든 절(節)에서 읽도록 고쳤다(`slots.py CLAUSE`).
  "원인불명 누수"를 확인 불가로 읽던 것도 고쳤다.
- p.148 의 "침식·도전로 형성"을 환경 슬롯에서도 탄화 도전로로 읽는다.

### 4.5 반증 후보와 검토표 — 사람에게 의견을 묻지 않는다

검토표 시트 6(반증 후보 30행)은 지표 여섯 개를 조사관에게 "맞습니까?"로 묻고 있었다.
그 질문은 의견이 아니라 사실("A 도 X 를 내는가")이고, 원문 대조 감사(audit·cause_audit)가
이미 답했다. 원문 인용은 전부 "B 가 X 를 낸다"이지 "X 가 있으면 A 가 아니다"가 아니며,
합금 변색은 화재 자체가 내는 흔적이다. `make refute` 로 사례 반례를 세면 관통부 마모가
반단선 1건에서 정답을 깎고 나머지는 관측이 없다. **하나도 붙이지 않았다.**
`test_refuting_rules_have_no_counterexamples` 가 앞으로 붙는 반증을 감시한다.

검토표 시트 2·3·4·6 을 전부 **반례 수집 형식**으로 바꿨다 — "그 반대인 사건번호"를
받는 '반례 사건번호' 열. 의견은 사슬에 들어가지 못하고 사건은 들어간다.

### 4.6 국내 표 2-1 의 '없음' 3건은 범위 밖으로 닫았다

고조파 과전류(p.246·255, 고압 콘덴서 과열만, 판정 기준 없음, 과부하 갈래), 반도체
전기적 파괴(본문 없음), 은 이동(p.154 조건·감식 요령 있음, 직류기기 현상). 셋 다
다섯 요인 밖이다. 이식률 42 → 50% 는 분모가 준 것이지 구현이 는 것이 아니다.

---

## 5. 남은 일 — 무엇이 누구 몫인지

### 5.0 실무Ⅳ 목차 대조에서 나온 것 — 후보 7개 (2026-09-02 추가)

5.1 의 "더 갈 곳이 없다"는 **사례 150건 기준**이다. 원문 기준으로는 다르다.
TTL 이 인용한 실무Ⅳ 쪽은 357쪽 중 22쪽이었고 전부 낱말 검색으로 찾아간 곳이었다 —
검색어를 떠올리지 못한 개념은 검색되지 않았다. 그래서 제2편(p.126~342)을 표제 글꼴로
잘라 절 43개·하위 절 115개를 하나씩 판정했다(`scripts/silmu.py`, `make silmu`).
nfpa·domestic 과 달리 **'없음'을 남긴다** — 원문이 감식 기준을 주는데 어휘가 없는 것.
하위 절 8개가 없음이고 후보 7개로 묶인다. 우선순위 순:

1. **극별 용융 위치** (p.185 ①·⑦㉰·⑧). 한 극만 용융 → 접촉불량·반단선·지락, 양극 →
   트래킹. 같은 기준이 감식 포인트에 세 번 나온다. **세어 봤다 — 서식 문제다.**
   조사서 150건에 '한 극'은 0건, '양극'은 1건(UIJEONGBU_2025_055, 서모스탯 접속부
   양측 쌍 용융 — p.185 ㉯ 그대로). 접속부·플러그·기판 근처 용흔을 적은 28건은 극을
   적지 않는다. 규칙을 붙여도 발동할 사례가 없으니 어휘를 만들지 않는다(CLAUDE.md —
   어휘만 만들고 사슬에 안 붙이는 실패). 5.3 서식 항목으로 옮겼다.
2. **탄화도전로 도통·저항 수Ω~수십Ω** (p.243·251·272·299). 네 기기에서 반복된다.
   **세어 봤다 — 0건.** 서식으로(5.3).
3. **쥐 갉음 → 반단선** (p.286). `RodentGnawing` 이 압착손상에만 이어져 있다.
   **세어 봤다 — 쥐 0건.** 사례와 무관한 사슬 오류이므로 원문대로 고친다. cause_audit
   EXCL → SHARED, `enables` 추가, 반단선 규칙 추가 후 `score.py --write`. 정확도 불변.
4. **금속 개입 단락·꼬아 접속의 차단기 미동작** (p.285·286). **`make refute`: `R_CD_ref`
   발동 0·반례 0.** 금속 접촉 압착 1건(SUWON_2024_167)은 "일부 트립"으로 읽혀 안 걸린다.
   그러나 p.285 는 압착(금속 개입) 단락에서 트립 없음이 **흔하다**고 — 양립을 — 말한다.
   반증은 원문이 양립 불가를 말할 때만 둔다(CLAUDE.md, 7장 ⑨). `R_CD_ref` 의 출처는
   논문 Table 2 이고 실무Ⅳ 가 반대로 적는다. **지웠다** — 규칙·`NoTripDespiteExternalForce`
   클래스·'트립 없음 → 비통전' attests 전부. 대신 p.150 "양측 용흔이면 금속 절단이
   아니다"를 `R_CD_ref3` 으로 두어 `R_PD_ref3` 의 대칭을 만들었다. 가설당 반증 ≥2
   불변식은 그대로 통과한다. 사례 150건에 양측 용흔은 반단선 1건이라 반례 0.
5. **납땜 불량·균열** (p.185·222·237). **세어 봤다 — 1건, 그것도 '납땜 안 하고 꼬아
   마감'.** 서식으로.
6. **니크롬선 용융 1,425℃** (p.293). 외부화염 반증 또는 F-3. 열선 용융 2건은 둘 다
   절연열화의 열선 단락흔이지 니크롬 자체 용융이 아니다. 외부화염 라벨 0건. 후순위.
7. **통전 입증 단서** (p.178~179·214). **세어 봤다 — `EnergizedState` 는 이미 143/150
   확인**('사고 당시 통전됨' 55건 등). 6개 가설 전부 양립이라 지지 규칙은 `score.py`
   공식상 0점 — gaps.py 의 RULE 판정은 틀렸었다(NA 로 고침). **C-57
   `EnergizedRequiredShape` 로 넣었다** — 통전 확인 없는 전기적 결론을 막는다. 점수가
   아니라 문턱이다. 확인 불가도 막히되 기각은 아니다(C-1). 통전 미확인 7건은 조사서가
   정말 안 적은 것(62~451자짜리)이라 판독 문제가 아니다. SHACL 은 평가 루프 밖이라
   정확도는 82.7% 그대로. 칼날 광택 같은 단서 자체는 서식 항목.

**2단계 끝:** ③ 쥐 갉음 사슬(`40dc0bf`), ⑦ 통전 제약 C-57(`acf3ce9`), ④ `R_CD_ref`
교체. 셋 다 정확도 82.7% 그대로 — 원문대로 고쳤는데 숫자가 안 움직인 것은 사례가
그 자리를 안 적기 때문이다. 나머지는 서식 항목(5.3)이다. **3단계 서식 제안서 — `docs/조사서_서식_제안.md`.** 여섯 칸(대상·용융 극·탄화부 저항·접속 방식·통전·차단기)과 세 상태(확인됨/없음 확인/확인 불가). 검토표와 함께 조사관에게 건넨다. 붙일 때는 CLAUDE.md 의 두 질문(화재도 내는가 /
그 요인에서만인가)을 거치고 `python scripts/score.py --write` → `make test` →
`make calibrate` → `make refute`. **어휘만 만들고 사슬에 안 붙이는 실패(7장)를
되풀이하지 말 것.**

대조표의 근거 칸이 부르는 이름은 `test_silmu_table_names_existing_vocabulary` 가
TTL 과 묶는다. 클래스 이름을 바꾸면 여기서 걸린다.

원문 결함 하나: p.299 전기장판 "다. 감식사례" 본문은 HID램프 절(p.229)의 오식이다.

### 5.0b Babrauskas Ignition Handbook 대조에서 나온 것 — 후보 7개 (2026-09-02 추가)

`scripts/babrauskas.py`(`make babrauskas`). 실무Ⅳ와 달리 **국내 교재와의 관계 열**이 있다 —
범위 내 87건 중 일치 60 · 상충 12 · 보강 14. 상충이 값어치다. 우선순위 순:

1. **접점 융착은 화재도 낸다** (p.762 Béland). 통전 없이 목재 화재 노출만으로 접점·서모스탯·
   차단기 접점이 융착한다. `audit.py` 는 `ContactWelding` 을 ELEC(화재 못 냄)으로 뒀고
   `R_PC_sup8`(+10)이 그 위에 선다. **일곱 번째 형태 편향.** 실무Ⅴ p.238 "아크가 융착시킨다"를
   "화재는 못 시킨다"로 읽은 7장 ⑨ 류의 오독. 가르는 기준은 부품 소손 정도. → FIRE 로 고치고
   `score.py --write`. 사례에서 ContactWelding 관측 수를 먼저 센다.
2. **쥐 갉음·스테이플 → 트래킹** (p.787 Hosaka 흐름도, p.790). 배설물이 도전성이라 누설·트래킹도
   낸다. `RodentGnawing`·`MisdrivenStaple` 에 `ArcTracking` 을 잇는다. 쥐 0건이라 정확도 불변.
3. **열적 열화 → 트래킹, 오염 환경 없이** (p.758 Ashizawa, p.774 UL 4경로). 과열 접속부가 PVC 를
   열화시켜 염화칼슘이 대기 습기를 빨아들여 탄화 경로가 생긴다. `TrackingScenario` 의 필요조건
   `ContaminatedEnvironment`(C-5)가 이 경로를 못 덮는다. 실무Ⅳ p.196·3.1.5바도 같은 편.
   **설계 결정** — 넣으면 트래킹/절연열화 경계(4.3)가 흔들린다. 사례로 먼저 센다.
4. **최하류 = 최초 아크의 조건** (p.778 Bernstein). 강한 화재 노출 회로는 전 구간 동시 아크.
   D-5·실무Ⅳ p.185·305 의 '부하측이 1차'는 조건부다. 규칙은 두고 주석·메시지에 조건을 단다.
5. **나사 재질(강철/황동, 자석 판별)·체결 토크 0.7 N·m** (p.759·764). 관측 가능한 접촉불량
   선행조건. 토크는 dataset 슬롯이 있고 온톨로지 속성이 없다. 서식 항목 D 에 '나사 재질'.
6. **절연저항 정상은 트래킹을 반증 못 한다** (p.753 Cooper). `R_TR_ref`(−30)의 반례 사례.
   탄화 경로는 건조하면 고저항. 사례 발동 0. `make refute` 뒤 역할 재검토.
7. 열선(동파방지) 고장 모드 (p.854~856). 사례 열선 2건(절연열화)이 이 절. 어휘는 3건 미만이라 안 만든다.

**국내 교재와 갈리는 큰 지점 — 1차/2차 용흔 형태 판별** (p.795~804). cause/victim 비드는
광택·색·형상·평활도·크기로 가를 수 없고, 공극·수지상·산소 프로파일 등 제안된 방법 전부가
통계적으로 서지 않는다(65건 실화재 39% 일치). 실무Ⅳ p.308~310 의 경향 서술과 상충하나
실무Ⅳ 자신도 p.305·310 에서 "단독 판정 불가"라 적는다. 온톨로지는 F-2(시간 선후)와
`MetallographyAloneShape` 로 이미 Babrauskas 쪽이다. `voidFraction`·`fineDendriticGrain`
주석에 이 절을 인용할 것. **논문에서 한 절로 쓸 상충이다.**

이 표에서 다시 확인된 오늘의 결정 셋: 트립 없음은 반증이 아니다(p.745·774·778 — 화재도
83% 트립시킨다 p.786), 통전은 필요조건이다(p.757 무부하면 발열 없음), 쥐 갉음은 반단선도
만든다(p.787 소선 파단 과열).

### 5.1 온톨로지 몫 — 사례 150건 기준으로는 근거 있게 더 갈 곳이 없다

남은 거리 안의 사례를 전부 열어 봤다.
- **반단선 7건 미판정**: 조사서가 빈약하다(비드만, 차단기만, 아무것도 없음). "장력
  **또는** 날카로운 물체"처럼 조사관 스스로 갈라 적지 않은 것도 있다.
- **절연열화 4건**: 진동 + 노후 3자 동점 둘, 실외기 단자함 + 습기(GOYANG_111 — p.149
  관례대로 트래킹이 이긴다. 라벨과 이론이 갈린 자리) 하나.
- **트래킹 3건**: "단자에서 용융흔"처럼 낱말이 한 글자 모자란 판독. 사전 패턴으로
  잡을 수는 있으나 과잉 판독(⑦)과의 경계다.

**calibrate 신호**: 염해·열적 열화가 유도 9 vs 실측 15. 표본이 1건·3건이라 손대지 않았다.

### 5.2 원문이 있어야 하는 것 — NFPA 921

'일부' 9개 절 중 §9.4(Service Equipment) · §9.7(Branch Circuits) · §9.8(Outlets and
Devices) · §9.12(Electrical System Examination) 는 절 제목 수준의 근사치다. 저작권
표준이라 내가 받을 수 없고, 사용자가 앞서 챕터 이미지로 §4.1~4.7 · §9.3~9.13 ·
§19.4~19.8 을 주었으니 그 방식이면 된다. **정확도는 거의 안 움직인다** — 절차 층이다.

### 5.3 사람이 있어야 하는 것

- **검토표 회수** (`build/EFI-Onto_검토표.xlsx`). 반례 사건번호 형식이라 의견이 사슬에
  들어갈 입구가 없다. 시트 2·3·4 는 사슬을 바꾸므로 가중치가 바뀌고 방향은 보장되지
  않는다. 시트 5(용어)는 정확도와 무관하다.
- **조사서 서식에 '대상' 항목.** 지금은 판독기가 관찰 전문에서 뽑는다(121/150).
  서식에 있으면 판독 없이 채워진다. 트래킹·절연열화·반단선의 동점 상당수가 이것으로 갈린다.
- **조사서 서식에 '용융 극' 항목 — 한 극 / 양극 / 미확인.** 실무Ⅳ p.185 가 세 번 반복하는
  기준(한 극 → 접촉불량·반단선, 양극 → 트래킹)인데 조사서는 적지 않는다(150건 중 1건).
  접속부 용흔을 적은 28건이 그 자리에서 보기만 하면 채울 수 있는 칸이다. 채워지면 그때
  어휘와 규칙을 만든다 — 그 전엔 만들지 않는다.
- **조사서 서식에 '탄화부 저항·도통' 항목.** 실무Ⅳ 가 네 기기에서 시키는 계측(p.243·251·
  272·299)인데 150건에 0건. 트래킹의 계측 확인이 된다.
- **조사서 서식에 '접속 방식' 항목 — 납땜 / 커넥터 / 꼬임 / 나사.** 납땜 불량(p.185·222·237)은
  1건도 적히지 않았고, 꼬임 마감은 접촉불량의 자리(`ConnectionJointSite`)다.

---

## 6. 온톨로지 몫을 파는 절차

0. **`make silmu`** — 원문 목차 단위로 '없음'이 어디인지 본다. 사례가 아니라 원문이
   출발점이다. 사례로만 파면 조사서가 안 적는 것은 영원히 안 보인다.
1. **`make readers`** — 판독이 병목인지 먼저 가른다. 차이가 크면 온톨로지를 고칠 때가 아니다.
2. **`python scripts/eval.py` 의 상한·남은거리** — 온톨로지 몫과 조사서 몫을 가른다.
3. **남은 거리 안의 사례를 연다** — 슬롯 텍스트가 아니라 `cases/observed` 전문을 읽는다.
4. **핵심 단서 교차표** — 동점이 왜 생기는지 보인다.
5. **원문 PDF 전체를 검색한다** — 한 쪽의 인용으로 규칙을 붙이거나 빼지 않는다(⑪).
6. **`make calibrate` · `make refute`** — 어긋나면 값이 아니라 사슬을 다시 본다.

---

## 7. 반복해서 저지른 실수 — 다시 하지 말 것

이 목록이 이 문서에서 가장 값어치 있는 부분이다. 전부 실제로 일어났다.

**① 사전이 못 읽은 것을 조사서가 안 적은 것으로 착각했다 (3회)**
표기 변이였다 — 수분/습기, 불완전/느슨, 꼬여/꼬아, '용융 흔적'/'용융흔', 연식 표현,
"늘어남과 줄어듦이 반복"(인장).
→ **`make readers` 로 먼저 가른다.** 상한이 오르면 판독 문제다.

**② 상위 개념을 하위의 형제로 두었다 (3회)**
장기 반복 응력, 절연 열화, 오염 환경. 조사서는 '굴곡'이라 적지 '장기 반복 응력'이라
적지 않는다.
→ **새 상위 개념을 만들면 `owl:unionOf` 와 `rdfs:subClassOf` 를 둘 다 적는다.**

**③ 원문이 'A 도 되고 B 도 된다'고 적은 것을 A 쪽으로만 이었다 (여러 회)**
진동, 플러그 접속부, 먼지·습기, 노후, 마찰.
→ `scripts/cause_audit.py` 가 센다. **새 선행 조건을 한 가설에만 이을 때 반드시
묻는다: 정말 그 요인에서만 일어나는가?**

**④ 합성 개념을 요구하고 실무 어휘를 버렸다 (3회)**
`StrandFractureAtStressPoint` 만 요구하고 조사서가 쓰는 '소선 일부가 끊어진'을 버렸다.
→ 규칙을 만들 때 **조사서가 실제로 쓰는 말**을 확인한다.

**⑤ 규칙 id 와 C-번호를 재사용했다 (각 3건)**
RDF 는 같은 id 를 한 자원으로 합친다. 시험이 전부 통과하는 동안 지표가 조용히 사라졌다.
→ `test_each_indicator_rule_says_one_thing` · `test_constraint_numbers_are_unique`.

**⑥ Python 만 고치고 SHACL 을 안 고쳤다 (3회)**
부재 확인의 포섭 방향, 지표 해상도 규칙, 그리고 반증 처리(F-1).
→ **점수·판정·도출을 고치면 `efi_schema` 와 SHACL 을 함께 고치고 두 엔진을 견주는
시험을 붙인다.** 지금 셋 있다(점수·판정·D-13).

**⑦ LLM 판독으로 과잉 판독했다 (8건)**
사전은 '못 읽는' 쪽으로, LLM 은 '과하게 읽는' 쪽으로 틀린다. **후자가 더 위험하다** —
오판이 늘고 그 오판이 온톨로지 오류처럼 보인다. 슬롯을 넘나드는 추론도 같은 부류라
대상 슬롯을 따로 만들었다.

**⑧ bash heredoc 안에서 `\n` 이 실제 줄바꿈이 됐다 (6회 이상, 이번 세션에도 1회)**
→ 파이썬 소스를 고칠 때는 **Edit 도구**를 쓰거나 줄 단위로 다룬다. 백슬래시를 넣어야
하면 `chr(92)` 로 만든다.

**⑨ 원문 인용이 있다고 반증 근거가 있는 것은 아니다 (1회)**
"B 가 X 를 낸다"는 인용을 "X 가 있으면 A 가 아니다"로 읽었다. 기존 반증 선례는 같은
자리에서 물리적으로 양립 불가한 상태였다.
→ **반증을 붙이기 전에 인용이 양립 불가를 말하는지 확인하고 `make refute` 로 반례를 센다.**

**⑩ 규칙 순서를 도형 안에서만 적었다 (1회)**
→ **규칙 도형에는 도형 수준 `sh:order` 를 함께 적는다.** 시험이 잡는다.

**⑪ 원문 한 곳만 읽고 결론을 두 번 뒤집었다 (같은 세션에서 2회)**
p.245 만 보고 "모터 한정"이라 했다가 p.196·201 이 일반 목록인 것을 나중에 찾았다.
→ **규칙을 붙이거나 뺄 때 PDF 전체를 검색한다.** 정확도의 오르내림을 판정 근거로
쓰지 않는다 — 두 번 다 그 유혹이 있었다.

**⑫ 부재 어휘를 슬롯 전체에 걸었다 (1회)**
"A 식별됨, B 는 없음"에서 A 까지 부재로 읽었다.
→ **확인 상태는 그 표현이 든 절에서 읽는다.**

---

## 8. 알아 둘 설계 결정

**판독기를 둘로 둔다.** 사전(`slots.py`)은 투명하고 재현되지만 표기 변이에 약하다.
LLM 판독(`cases/llm_reading.json`)은 유연하지만 과잉 판독한다. 대체하지 않고 나란히
두어 `make readers` 로 차이를 잰다. LLM 판독은 **근거(`why`)를 반드시 함께 적는다.**

**점수는 원문이 그 요인의 표지로 열거한 가설만 받는다.** 사슬의 양립 수 k 는 값의
크기만 감쇠한다. 규칙이 있는 지표 중 다수가 '양립하지만 규칙은 한쪽에만' 인 비대칭이며
그것은 설계다. `scripts/score.py` 머리말.

**반증 단서는 감점, 기각은 결정적 반증만.** 누적돼 40 미만이면 약화.

**도출은 새 사실을 만들지 않는다(C-4).** 기존 사실에 유형을 덧붙인다(F-2, D-13).

**숫자는 규칙에 박지 않는다.** 절연저항(D-9), 반단선 10%(D-11), 차단기 동작시간(D-12)
은 기준값을 대상의 속성으로 두고 규칙은 견주기만 한다.

**한 관측은 한 번만 센다.** 더 구체적인 규칙이 함께 발동하면 상위 규칙은 세지 않는다.

**확장 슬롯과 미부착 계측 속성은 '결정됨'으로 처리했다.** `lint` 가 목록은 보여 주되
미완성으로 세지 않는다.

**발화개소 판정은 범위 밖이다** (설계계획서 1.1).

---

## 9. 원문 자료

**구해야 할 자료의 목록과 구하는 법은 `docs/자료_목록.md`.** **Babrauskas Ignition Handbook 은 확보했고 대조표를 만들었다**(5.0b, `scripts/babrauskas.py`). 절 위치는 자료_목록 1-1. 텍스트 추출은 pymupdf 로 bold 11pt 표제를 잡았다. 우선순위는 Babrauskas → 아크 논문 → 국내 → 일본 원전.

스크래치패드 캐시는 세션이 바뀌면 사라진다. 링크로 다시 받는다.

- 게시물: https://www.nfa.go.kr/nfsa/releaseinformation/archive/materials/?boardId=bbs_0000000000000035&mode=view&cntId=106779
- **화재조사실무Ⅳ (2025)** 직접 링크(2026-09 기준 동작, 7.8 MB, 357쪽):
  https://www.nfa.go.kr/board/file/bbs_0000000000000035/651129/FILE_000000000023665/2025011716141803050
  **인쇄 쪽수 = PDF 쪽 − 13** (p.147 → PDF 160). pypdf 로 텍스트가 잘 뽑힌다.
  **목차는 pypdf 로 안 뽑힌다** — 표제가 본문과 한 줄로 붙는다. pymupdf 의 span 글꼴
  크기로 가른다: 13.5pt 절, 12.1pt 'N.', 10.8pt '가.', 9.7pt 본문. `silmu.py` 의
  절 목록이 그렇게 만들어졌다. 이전 세션 스크래치패드에 캐시가 남아 있을 수 있다
  (`%LOCALAPPDATA%\Temp\claude\V--Projects-efi-onto\*\scratchpad\efi4.pdf` 또는 `silmu4.pdf`).
  이번 세션에 쓴 곳: p.127 표 2-1, p.147~149 트래킹·흑연화·반단선, p.153~154 아산화동·
  보이드·은 이동, p.196·201 차단기 절연열화의 원인 목록, p.245~247·255·259 전기기기,
  p.289 콘덴서, p.305 1차 용흔.
- **실무Ⅳ의 원전 (2026-09-02 확인).** 제2편에는 참고문헌 목록이 **없다.** 제1편(화학) 끝
  p.125 의 목록이 유일하며 전부 일본 東京消防庁 감수 교재다 — 『신화재조사교본』(동경방재지도협회
  1997, 제1권·제4권), 『화재조사기술교본』(동경방재지도협회 1987 제4권 · 동경법령출판 1986 제2권),
  『화재조사』(소방과학종합센터 1996, 일본소방대학교 감수), 『화재실태』(2000). 제2편의 트래킹·
  그라파이트(金原壽郞, p.150)·은 이동·기기별 감식사례 서술은 같은 계열의 번역으로 보인다 —
  『火災調査技術教本』 전기편이 원전 후보. 제2편 본문 인용은 넷뿐: 『전기화재감식공학』(p.184
  "Chapter 8 이후 참조" — 국내 단행본), 한국전기안전공사 「배선용 및 기기용 전선의 화재위험성에
  관한 연구」(산업자원부 2001.12, p.318 동 전선 온도별 사진), KS C 8321·4613, IEC 60085·62305.
  판권면: "2018년도 교재를 기반으로 개발", 집필 한영수, 검토 최규출·손원배·천성수·황인환.
- 화재조사실무Ⅰ — 표 4-9(단락흔과 열흔). 실무Ⅴ — p.236 불완전 접촉 발열의 세 갈래.
  같은 게시물의 첨부다.
- NFPA 921 (2024) — 저작권 표준. 사용자가 이미지로 준다.

---

## 10. 데이터

- `V:/Dataset/dataset.xlsx` — 사례 150건, 슬롯 10개. 5요인 30건씩 균등 표집.
- `cases/raw/` — 원본 HWPX 150건 (git 제외)
- `cases/observed/` — 관찰 전문 텍스트 150건 (git 제외). 대상 슬롯은 여기서 읽는다.
- `cases/sessions.json` — 사전 판독 결과 (git 제외, `slots.py` → `enrich.py` 로 재생성).
  사례마다 `origin_object` 가 있다.
- `cases/llm_reading.json` — LLM 판독 기록 (git 포함)
- `cases/baseline.json` — 정확도 스냅숏 (git 포함)

**라벨은 조사관 판정이다.** 정답이 아니라 사람의 판단이며, 5요인 균등 표집이라 실제
발생 분포도 아니다. 정확도를 읽을 때 함께 읽을 것.
