# TBox 재편 계획 — 절 순서와 기록 도형의 분리

작성 2026-09-04. 대상은 구조 문제 둘이다.

1. TTL 이 주제가 아니라 작업 순서로 쌓여 있다 (58절, 5339줄. 29·35·36·41·47절은 고친 기록이 절 제목). — 2단계
2. T01~T16 의 기록 도형이 추론 어휘와 한 파일에 섞여 있다. — **1단계에서 해결**

## 원칙

- **의미를 바꾸지 않는다.** 재편 전후 그래프가 동형(isomorphic)이어야 한다. 이것이 유일한 완료 조건이다.
- **손으로 옮기지 않는다.** 주어 블록 단위로 스크립트가 옮긴다. 5000줄을 손으로 잘라 붙이면 반드시 빠진다.
- **규칙 순서는 파일 순서가 아니다.** `sh:order` 가 정한다. 그래서 블록을 옮겨도 판정이 안 바뀐다. 이것을 0단계에서 시험으로 확인한 뒤 시작한다.
- **줄 번호로 가리키지 않는다.** 문서·backlog 는 앵커 문자열로 가리킨다. `scripts/backlog.py` 는 이미 그렇게 한다.
- **한 단계에 한 커밋.** 각 단계 끝에 `make test` · `make lint` · `make reason` · 동형 검사가 통과해야 한다.

## 0단계 · 기준선 — 완료 2026-09-04 (커밋 `b0b9ec8` 위)

T16 커밋(`b0b9ec8`) 뒤에 시작했다. 계획대로 넷을 했고 예정에 없던 것 둘이 나왔다.

1. `scripts/isomorphic.py` — `make iso` / `make iso-freeze`. 기준선은 `build/baseline.json`(git 밖). 재편을 시작하기 전에 굳힌다.
2. `scripts/ttlblocks.py` — 주어 블록 파서. 2단계의 도구다. 왕복(cut → join)이 원본과 바이트 단위로 같아야 하고 실행하면 그것부터 확인한다. 현재 블록 1371개, 주어 있는 것 1350개.
3. **순서 무관성 증명 완료.** 씨앗 셋으로 본문 1355 블록을 섞어 전부 동형이었고, 씨앗 7 로 온톨로지를 바꿔 치고 시험 204개 전부 통과, 150건 평가 결과가 기준과 한 글자도 다르지 않았다. **파일 순서에 기대는 곳은 없다.** 2단계에서 블록을 옮겨도 판정은 안 바뀐다.
4. `scripts/_utf8.py` — stdout·stderr 를 utf-8 로 고정한다. CLI 스크립트 20개가 부른다. `status.py` 가 em-dash 하나에 죽던 것이 고쳐졌다.

### 0단계에서 드러난 것 둘 — 계획에 없던 것

**rdflib 의 `to_isomorphic` 은 이 그래프에 못 쓴다.** 빈 노드 932개 중 `sh:property [ sh:path …; sh:minCount 1 ]` 처럼 구조가 닮은 것이 많아 정규화가 수렴하지 못한다. 블록 순서만 바꿔도 '동형 아님'을 내는데 접지 트리플 4716개는 전부 같았다. 그래서 빈 노드마다 재귀 서명을 만들어 다중집합으로 비교하도록 바꿨다. 이 그래프의 빈 노드는 명명 주어에 매달린 나무라서 가능하다.

**CRLF 가 SPARQL 규칙의 값을 바꾼다 — `.gitattributes` 를 더했다.** `core.autocrlf=true` 인데 `.gitattributes` 가 없어서, 저장소는 LF 로 두고 체크아웃할 때 CRLF 로 바꿨다. 삼중따옴표 안의 SPARQL 문자열이 통째로 리터럴이므로 **같은 커밋인데 그래프가 달라진다** — 실제로 134개 트리플이 갈렸다. 시험은 통과한다(SPARQL 이 `\r` 을 공백으로 보므로). 그러나 이 상태로는 동형 검사가 성립하지 않고, Windows 에서 클론한 사람은 시험된 것과 다른 그래프를 받는다. `* text=auto eol=lf` 로 고정하고 `git add --renormalize` 를 돌렸다. 재편 스크립트도 반드시 `newline="\n"` 으로 쓴다.

## 1단계 · 기록 도형 분리 — 완료 2026-09-04

파일을 둘로 나눴다. **그래프는 하나다** — `make iso` 가 동형(트리플 7854)이고 150건 평가도 한 글자 안 바뀌었다.

```
ontology/efi_tbox.ttl            추론 어휘. 3871줄, 블록 1101
ontology/efi_investigation.ttl   조사 기록 층 (T01~T16). 1503줄, 블록 372
```

기준은 **점수·반증·형성(D-15)·결론(C-)에 닿는가**였다. 50절부터 끝까지를 옮기되 결론 제약 둘(`ConclusionFirstFuelEvidenceShape`·`ConclusionHeatTransferEvidenceShape`)은 tbox 에 남겼다. 나머지 372 블록은 전부 기록의 완비만 본다 — 옮기기 전에 판정 관련 속성 열둘로 전수 검사해 확인했다.

1. `efi_schema.ontology_files()` · `load_graph()` · `ontology_text()` 가 파일 목록의 단일 진실 원천이다. 손으로 파일 이름을 든 곳 40여 군데를 이 셋으로 바꿨다. `Ontology.load()` · `load_rules()` · `Session.validate()` 의 경로 인수는 선택이 됐고, 주지 않으면 전체를 읽는다.
2. HermiT 은 `consistency.py` 가 이미 한 그래프로 합쳐 RDF/XML 로 넘기므로 별도 bundle 이 필요 없었다.
3. `make lint` 가 기록 어휘를 **'기록 전용 어휘' 353개**로 따로 센다. 구조 결함(합계 0건)과 분리해 보이므로 기록 층이 자라는 것이 숨지 않는다.
4. 시험 둘을 더했다 — `test_investigation_layer_never_touches_scoring`(경계), `test_every_ontology_file_is_loaded`(파일 하나가 통째로 빠지는 것).
5. `docs/tbox_backlog.json` 의 근거 13개가 새 파일을 가리키도록 고쳤다. 앵커 문자열은 그대로다.

### 1단계에서 드러난 것 — 블록 파서의 결함

**줄 단위 파서가 블록 102개를 뭉쳐 놓고 있었다.** SPARQL 규칙을 닫는 따옴표와 문장의 마침표가 한 줄에 같이 오는데, 줄 단위로는 그 마침표를 놓친다. **왕복 검사도 섞기 검사도 이것을 잡지 못했다** — 뭉친 블록은 뭉친 채로 함께 움직이므로 둘 다 통과한다. 실제로 D-15(가설 형성)가 50절의 기록 클래스에 붙어 있었고, 그대로 옮겼으면 판정 규칙이 기록 층으로 넘어갈 뻔했다. 문자 단위 상태 기계로 바꿔 블록이 1371 → 1473 이 됐다. **2단계는 이 파서에 전적으로 기대므로, 파서를 고치면 섞기 검사를 다시 돌린다.**

## 2단계 · 절 재편 — 완료 2026-09-04

절을 주제로 다시 묶었다. **동형이다** — `make iso` 통과, 시험 206개 통과, 150건 평가 동일, DL 일관성 통과.

### 실제 배치

절 번호는 순서가 아니라 자리다. 앵커는 `# [7.3]` 꼴이고 문서·backlog 가 이 문자열로 가리킨다.

| 절 | 내용 | 블록 |
|---|---|---:|
| `[0]` | 머리말 · 접두어 | 16 |
| `[1]` | 바탕 — BFO 정렬, 관측·출처·시간, 세션과 상태 열거 | 53 |
| `[2]` | 축 ① 발열 메커니즘 | 146 |
| `[3]` | 축 ② 물리 증거물 · 손상 양상 | 169 |
| `[4]` | 축 ③ 선행 조건 | 163 |
| `[5]` | 축 ④ 최초 착화물 | 30 |
| `[6]` | 속성 | 128 |
| `[7]` | 가설 공통 — 배타 공리 | 1 |
| `[7.1]`~`[7.9]` | 가설 아홉. 정의 · 필요조건 · 형태 발현 · 지표 규칙 | 178 |
| `[8]` | 방법론 층 — M · F · C · D 도형과 절차 어휘 | 217 |
| `[9]` | 미분류 | **0** |

### 계획과 달라진 것 넷

1. **절 이름을 문자에서 번호로 바꿨다.** `[C]`·`[D]` 가 기존 제약 도형 `C-39`·`D-1` 과 눈으로 구별되지 않았다. 처음 써 보고 되돌린 뒤 번호로 다시 했다.
2. **인과 사슬 절(계획의 I)을 두지 않았다.** `enables`·`canManifest` 선언은 주어가 사는 절로 간다. 오염 환경이 무엇을 가능케 하는지가 그 클래스 정의 바로 옆에 오므로 따로 모으는 것보다 낫다.
3. **NFPA 조항 절(계획의 K)은 비어서 없앴다.** 조항 인용은 `dcterms:references` 로 도형 자신에게 붙어 있어 도형과 함께 `[8]` 로 간다.
4. **파일 머리말을 다시 썼다.** 배너를 전부 이력으로 보내다가 파일의 머리말까지 사라졌다. 되살리면서 앵커 규칙과 `sh:order` 주의를 적었다.

### 드러난 것 — 같은 주어가 665 블록에 흩어져 있었다

주어 203개가 여러 블록으로 나뉘어 있다. 가장 심한 것은 외부화염 메커니즘과 가설이 각각 **24 블록**이다. 재편으로 한자리에 모였고 목록은 `docs/TBox_절_이력.md` 에 있다. **합치는 것은 3단계다** — 합치면 서로 어긋난 주석이 드러날 텐데, 2단계에서 손대면 동형이 깨져 무엇이 무엇을 바꿨는지 알 수 없다.

### 되돌아가지 않게

- `test_indicator_rule_lives_in_its_hypothesis_section` — 지표 규칙 104개가 자기 가설 절 안에 있다. 새 규칙을 파일 끝에 덧붙이면 잡힌다.
- `test_every_section_anchor_is_present` — 앵커 17개가 다 있다.

## 3단계 · 참조 갱신과 어긋남 정리 — 완료 2026-09-04

**끝난 것.** `backlog.py --write` 로 줄 번호를 다시 만들었고 앵커 오류 0. `CLAUDE.md` · `docs/인계.md` · `tbox_backlog.json` 의 '48절' '6절' '32·33절' 같은 표현을 `[7.7]` `[2]` 꼴 앵커나 어휘 이름으로 바꿨다. 재편 전 절 제목은 `docs/TBox_절_이력.md` 로 옮겼다.

### 3-a · 한글 이름이 둘인 자원 58개 — 완료 2026-09-04

중복을 합치기 전에 먼저 드러난 어긋남이다. 라벨을 나중에 한 번 더 붙이는 통에("부가 모듈 A") 자원 58개가 `skos:prefLabel` 을 둘씩 가졌고, 검토표·구조도(`build`)는 `rdfs:label` 을 읽어 **둘째 이름을 조사관에게 보여 줬다.** 판독기는 자체 사전을 쓰므로 판정에는 영향이 없었다(150건 평가 동일).

원칙: **정의 줄의 원래 prefLabel 을 남기고 둘째 이름은 altLabel 로 내린다. 뜻이 다르면 altLabel 도 아니고 지운다.** 지운 셋 —

| 자원 | 지운 이름 | 왜 |
|---|---|---|
| `SecondaryArcMark` | 2차 수열흔 | 열흔은 통전 없는 흔적이다(실무Ⅰ 표 4-9). 단락흔(`ArcMeltMark`) 하위에 붙을 이름이 아니다 |
| `ElectricalState` | 통전 상태 | 비통전(`DeEnergizedState`)도 하위다 |
| `Ignitability` | 착화 역량 | F-3·C-31 이 발열원에 쓰는 말(competence). 연료의 성향과 섞인다 |

그래프 차이는 라벨 트리플뿐임을 술어별로 셌다(사라짐 label 58·prefLabel 58, 생김 label 58·altLabel 55). `test_one_korean_pref_label_per_resource` 가 되돌아가지 않게 막는다.

### 3-b · 남아 있던 소절 담장과 주인이 다른 주석 — 완료

2단계는 `# ===` 배너만 걷어 `#####` 담장 17개가 남아 있었다. 담장과 번호 제목 줄만 `TBox_절_이력.md` 로 보내고 원문 인용이 든 본문 주석은 그 자리에 뒀다. 파서가 앞선 주석을 다음 블록에 붙이는 탓에 **앞 블록 것이던 주석 다섯**이 엉뚱한 블록에 딸려 있었다(D-8 동점 설명이 외부화염 메커니즘 위에 있는 식). 주인에게 돌려보냈다. 소절 제목에 묻혀 있던 `[D-9]` 규칙 머리는 도형 위에 되살렸다. 주석만 움직여 동형.

### 3-c · 같은 주어의 블록을 정의 옆으로 — 완료

주어 183개의 블록을 첫 선언 뒤에 원래 순서로 이었다. **문장 단위로 합치지는 않았다** — 외부화염 메커니즘의 `producesDamage` 24줄은 줄마다 감사 근거 주석이 붙어 있고, 합치면 그 근거가 뭉개진다. 그래프가 같고 읽는 자리가 하나면 목적은 이룬 것이다. 동형.

### 3-d · 라벨 전용 블록 126개를 정의에 접기 — 완료

'부가 모듈 A' 가 남긴 `X rdfs:label … ; skos:prefLabel … .` 블록들. 정의와 같은 prefLabel 을 되풀이하던 64개는 **같은 트리플이 텍스트에 두 번** 있어 rdflib 이 조용히 합치고 있었다. 정의 블록 끝에 접어 넣었다. 블록 1101 → 975. 비라벨 되풀이는 하나뿐이었다(절연열화 `canManifest InsulationCarbonization`) — 지웠다. 동형.

### 3-e · 다른 부모에 함의되는 군더더기 부모 22개 — 완료

한자리에 모이니 보인 것이다. 클래스 22개가 명명 부모를 둘씩 가졌는데 한쪽이 다른 쪽에 포섭된다 — `FirstFuelAssessment ⊑ InvestigationRecord` 정의 줄 아래 `⊑ InvestigationEvidenceRecord` 를 한 줄 덧붙인 꼴(14개), 한 줄에 둘을 나란히 적은 꼴(8개). 정의 줄의 부모를 좁은 쪽으로 바꾸고 덧붙인 줄을 지웠다. **포섭 폐쇄가 전후 동일**함을 확인했고 HermiT 통과. 트리플 7851 → 7829. BFO·SOSA·PROV 상위 정렬의 다중 부모는 의도라 둔다. `test_no_redundant_named_superclass` 가 지킨다.

### 3단계에서 찾은 어긋남 — 정리

| 종류 | 개수 | 처리 |
|---|---:|---|
| 한글 이름 둘 | 58 | 원래 이름 유지, 둘째는 altLabel. 뜻이 다른 셋은 삭제 |
| 주인이 다른 주석 | 5 | 주인 블록으로 |
| 같은 트리플 텍스트 되풀이 | 65 | 접기·삭제 |
| 군더더기 부모 | 22 | 좁은 부모만 남김 |
| 같은 자원에 주석 둘 | 0 | — |

**합치기 전에 걱정한 '앞뒤가 어긋난 주석'은 없었다.** 어긋남은 라벨과 계층에 있었다. 셋 중 하나는 조사관에게 잘못된 이름("2차 수열흔")으로 나갔던 것이다.

## 4단계 · 되돌아가지 않게 — 완료 2026-09-04 (2단계와 함께)

- `test_indicator_rule_lives_in_its_hypothesis_section` — 지표 규칙 104개가 자기 가설 절 안에 있다.
- `test_every_section_anchor_is_present` — 앵커 17개가 다 있다. 미분류 절은 아예 만들지 않으므로 여기 없으면 그것이 비었다는 뜻이다.
- `test_investigation_layer_never_touches_scoring` — 1단계에서 더했다.
- `test_every_ontology_file_is_loaded` — 1단계에서 더했다.

## 하지 않는 것

- 파일을 셋 이상으로 나누지 않는다. 가설별 파일은 교차 선언(enables 등)이 어디 있는지 찾기 어렵게 한다.
- 어휘 이름을 바꾸지 않는다. 이름 변경은 사례 150건·문서·Python 을 다 건드린다. 별도 작업.
- 2단계에서 의미를 고치지 않는다. 동형이 깨지는 순간 재편과 수정이 섞여 무엇이 무엇을 바꿨는지 알 수 없다.
- 3단계에서도 문장 단위 병합은 하지 않는다. 줄마다 붙은 감사 근거가 뭉개진다. 동형이 유지되면 텍스트 중복은 결함이 아니다.

## 위험

| 위험 | 대응 |
|---|---|
| Codex 가 같은 파일을 고치는 중 | 0단계 전에 커밋을 받는다. 재편 중에는 TTL 작업을 멈춘다. 2단계는 하루 안에 끝낸다 |
| 빈 노드 때문에 동형 검사가 실패 | 해결. `to_isomorphic` 대신 재귀 서명을 쓴다 (0단계) |
| pySHACL 이 파일 순서에 기댄 곳 | 없음을 확인했다 (0단계 3번) |
| HermiT 가 분리 파일을 못 읽음 | 해결. `consistency.py` 가 한 그래프로 합쳐 RDF/XML 로 넘긴다 |
| 블록 파서가 조용히 뭉치거나 자른다 | 왕복·섞기 검사로는 안 잡힌다 (1단계). 파서를 고치면 블록 수의 변화를 보고 경계를 눈으로 확인한다 |
| 절 번호를 든 문서를 놓침 | 3단계에서 `[0-9]+절` grep. `make backlog` 가 앵커 없는 참조를 오류로 낸다 |
